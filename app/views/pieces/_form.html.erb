<%= form_with(model: piece) do |form| %>
  <% if piece.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(piece.errors.count, "error") %> prohibited this piece from being saved:</h2>

      <ul>
        <% piece.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :pieceowner_id %>
    <%= form.text_field :pieceowner_id %>
  </div>

  <div class="field">
    <%= form.label :teamcolor %>
    <%= form.text_field :teamcolor %>
  </div>

  <div class="field">
    <%= form.label :piecetype %>
    <%= form.text_field :piecetype %>
  </div>

  <div class="field">
    <%= form.label :points %>
    <%= form.number_field :points %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
