<%= form_with(model: task) do |form| %>
  <% if task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
        <% task.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="field">
    <%= form.label :commenter_id %>
    <%= form.number_field :commenter_id %>
  </div>

  <div class="field">
    <%#= form.label :completed_id %>
    <%= form.hidden_field :completed_id %>
  </div>

  <div class="field">
    <%#= form.label :in_progress_id %>
    <%= form.hidden_field :in_progress_id %>
  </div>

  <div class="field">
    <%#= form.label :incomplete_id %>
    <%= form.hidden_field :incomplete_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
